{
  "name": "agent-holo-viz",
  "version": "0.1.0",
  "private": true,
  "description": "Holographic live visualizer for OpenClaw/Protheus system internals",
  "scripts": {
    "start": "node server/system_visualizer_server.js --host 127.0.0.1 --port 8787 --hours 24"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
